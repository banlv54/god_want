<%= form_for(@user_shop) do |f| %>
  <% if @user_shop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_shop.errors.count, "error") %> prohibited this user_shop from being saved:</h2>

      <ul>
      <% @user_shop.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :shop_id %><br>
    <%= f.number_field :shop_id %>
  </div>
  <div class="field">
    <%= f.label :role %><br>
    <%= f.text_field :role %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
